// import AsyncStorage from '@react-native-async-storage/async-storage';

export default class StorageGateway {
  // static Instance: StorageGateway | any = null;
  //
  // static getInstance() {
  //   if (StorageGateway.Instance === null) {
  //     StorageGateway.Instance = new StorageGateway();
  //   }
  //   return StorageGateway.Instance;
  // }
  //
  // get = (key: string): Promise<string | null> => {
  //   return AsyncStorage.getItem(key);
  // };
  //
  // saveString = (key: string, value: string) => {
  //   return AsyncStorage.setItem(key, value);
  // };
  //
  // saveObject = (key: string, objData: object | any) => {
  //   return AsyncStorage.setItem(key, JSON.stringify(objData));
  // };
  //
  // clear = (key: string) => {
  //   return AsyncStorage.removeItem(key);
  // };
}
